# 格式与编译核验

核验日期：2026-09-21。依据为本地附件2、附件3，以及用户确认的“附件2/3决定字体与版式，优秀论文只提供章节结构”。原附件和 `GMCMthesis-master` 均未修改。

后续用户调整：`main.tex` 将题目下划线加长为300 bp，左端仍为148.1 bp；题目中心右移27.485 bp。此项是用户要求的版面调整，与下述原Word题目区域的245.03 bp宽度不同。恢复原宽度可还原原始题目区。

此调整已重新以默认 `latexmk -pdf` 编译并检查摘要页，日志无警告和溢出。逐页渲染对比确认封面与全部正文页没有像素变化；修改限于摘要页题目文字的横向位置和下划线。当前四字题目实际中心为x=298.1 bp，下划线右端为448.1 bp。

GitHub发布前核验：把完整ZIP解压到独立目录，执行默认 `latexmk -pdf` 重新编译；六页输出与仓库 `example.pdf` 在同一渲染器下逐页像素一致，最终日志无警告、缺字与溢出。19个字体文件的SHA-256与清单一致。此项验证的是打包导入不改变现有LaTeX版式，不等于Word与LaTeX逐像素一致。

## 核验方法

1. 使用 Microsoft Word 只读打开附件3，导出官方参考PDF，提取页面、字形、段落、表格线和坐标。读取附件2的明确格式条款。
2. 扫描全部21篇、1780页优秀论文，详细查看A/B/E/F代表论文的首页、目录与正文。其字体、行距与目录并不完全统一，仅归纳结构。
3. 在临时 TeX Live 2026 环境实际编译新模板，并用另一独立输出目录执行默认编译模式测试。
4. 渲染并查看完整6页示例，再测试长题目、跨两页的摘要、44行正文、公式、三线表、交叉引用和附录编号。

## 页面与固定区

- Word导出的页面为595.5×842 bp，新PDF相同。
- 封面原Logo、华文新魏抬头、宋体标签、表格线和页脚0直接从Word矢量页面保留，没有重新绘制。
- 原PDF的个别Times New Roman资源未嵌入。从同一Word文件的PDF/A导出中补入相同字宽、编码的真实字体，避免Overleaf查看器临时代换。没有采用会改变Logo颜色的整页PDF/A替代。
- 摘要页固定抬头与题目标签、摘要标签均为原Word矢量内容。裁切区外的文字已真实移除，不通过隐藏整页封面来伪装为标题图。
- 抬头、题目标签和摘要标签的字体、字号、横纵坐标与参考一致。
- 单行题目基线自顶263.15 bp，16 bp SimHei；摘要标签基线325.70 bp；摘要首行基线356.73 bp。
- 页脚9 bp Times New Roman，基线自顶790.975 bp，单数字中心x=298 bp，摘要从1开始。
- 长题目保持16 bp字体，按20.8 bp行高向下增加空间，摘要标签和摘要正文同步下移。

## 正文

- 文字左边界x=64.05 bp，首行x=88.05 bp，首行缩进24 bp。
- 正文12 bp SimSun，一级标题14 bp SimHei居中，二级与三级12 bp SimSun。
- 以附件3的312 twips文档网格和Word单倍行距满页试验校准，使用15.6 bp连续行距；正文有效区域支持44行。
- 正文续页首行基线97.05 bp；44行测试末行基线767.85 bp；不进入页脚区域。
- 表题在上、图题在下，标题为12 bp宋体；参考文献标题14 bp黑体，条目12 bp宋体，方括号编号。
- 附录标题“附录 A”，子节“A.1”。

## 默认编译测试

在项目根目录运行以下Overleaf等效命令已成功：

```text
latexmk -pdf -interaction=nonstopmode -halt-on-error -synctex=1 -output-directory=build -jobname=output main.tex
```

虽然调用的是默认 `-pdf` 规则，日志确认根目录 `latexmkrc` 自动执行 `xelatex`，无需用户切换下拉菜单。配置也与当前Overleaf官方上游编译器调用方式核对。此处是本地等效验证，没有在用户的Overleaf账户中创建或修改项目。

最终结果：

- 主稿6页，实际产物为 `example.pdf`。
- 最终日志没有 LaTeX Warning、Overfull/Underfull、Missing character、字体替换或未定义引用。
- 递归检查正文与背景的PDF资源，所有使用字体均已嵌入。
- 54字长题目测试为4行，未与抬头、摘要标签重叠。
- 64行摘要自然分成两页，续页从正常正文顶部开始。
- 44行正文恰好占一页，公式、三线表、图题和引用测试通过。

## 精确一致性的范围

固定背景资源与Word对应区域在144 dpi下可逐像素一致。经XeTeX写入最终PDF后，同一渲染器、同分辨率比较，最终封面/抬头最大颜色通道差为2/255，全图平均绝对差约0.008/255；固定文字坐标和字号一致。因此不能把最终输出称为“每个像素都没有一点差异”。

Word导出会量化部分基线坐标，满页末行参考为768.45 bp，而LaTeX以精确15.6 bp连续行距得到767.85 bp。新正文采用官方名义单倍网格和相同字体，但Word与TeX对断行、标点压缩、公式、浮动图表及分页的算法不同。填入任意新内容后，不承诺与Word逐字断行、逐页分布或所有像素完全相同。

这份交付解决字体替换、固定模板重画、错误行距、封面删除和手动切换编译器的问题；最终论文仍应以实际填写内容的PDF检查结果为准。
